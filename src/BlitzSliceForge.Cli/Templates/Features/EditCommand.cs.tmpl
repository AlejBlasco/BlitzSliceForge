using MediatR;
using Microsoft.Extensions.Logging;
using {{ SolutionName }}.Shared.{{ FeatureName }};

namespace {{ SolutionName }}.Application.Features.{{ FeatureName }}.Commands;

public record Edit{{ FeatureName }}Command({{ FeatureName }}Dto {{ FeatureName }}) : IRequest<{{ FeatureName }}Dto?>;

public class Edit{{ FeatureName }}CommandHandler : IRequestHandler<Edit{{ FeatureName }}Command, {{ FeatureName }}Dto?>
{
    private readonly ILogger<Edit{{ FeatureName }}CommandHandler> logger;

    public Edit{{ FeatureName }}CommandHandler(ILogger<Edit{{ FeatureName }}CommandHandler> logger)
    {
        this.logger = logger
            ?? throw new ArgumentNullException(nameof(logger));
    }

    public Task<{{ FeatureName }}Dto?> Handle(Edit{{ FeatureName }}Command request, CancellationToken cancellationToken)
    {
        logger.LogInformation("Handling Edit{{ FeatureName }}Command for {{ FeatureName }}: {@{{ FeatureName }}}", request.{{ FeatureName }});

        // TODO: Implement data access logic to Edit the entity
        throw new NotImplementedException();
    }
}
